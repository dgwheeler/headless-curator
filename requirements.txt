# Core dependencies
httpx>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyyaml>=6.0.1
structlog>=24.1.0
apscheduler>=3.10.4
typer>=0.12.0
rich>=13.7.0
keyring>=25.0.0
pyjwt[crypto]>=2.8.0
aiosqlite>=0.19.0
sqlalchemy[asyncio]>=2.0.25

# Development dependencies (install with: pip install -r requirements.txt && pip install -r requirements-dev.txt)
# Or use: pip install -e ".[dev]"
